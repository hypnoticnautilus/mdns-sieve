# mdns-sieve Configuration File
# Modern, fine-grained, secure mDNS routing config

# Physical interfaces to bind and listen on
interfaces:
  - eth0
  - eth1
  - wlan0

# Default action if a packet does not match any of the rules below
default_action: deny

# Filtering rules evaluated in order (first match wins)
rules:
  # 1. Block Spotify Connect advertisements from eth1 going to wlan0
  - action: deny
    services:
      - "_spotify-connect._tcp.local"
    src: eth1
    dst: wlan0

  # 2. Allow Google Cast (Chromecast, Google Home) across all subnets
  - action: allow
    services:
      - "_googlecast._tcp.local"
    src: "*"
    dst: "*"

  # 3. Allow reflection of a specific Apple TV's records from eth0 to eth1
  - action: allow
    hosts:
      - "appletv.local"
    src: eth0
    dst: eth1
